// METHOD OF STRING

// length  - return string length
// slice -  return substring (accepts negative parameter)
// substring - return substring (swap krte h parameter ko agar ulta daalte h to) e.g-> string.substring(5,2)//2,5

//concat - string ke pichhe add krta h aur string
//trim - string ke dono side se space remove krega
//indexOf -
//lastIndexOf -
//includes - return true/false

/** replace & replaceAll
 
 *  replace() → only first match
 *  replaceAll() → all exact matches
  
 *  This is a regex (regular expression).
 * 
 *  regex with replace() -> console.log(str.replace(/apple/gi, "orange"));  
 * 
 * 
 *   2. /apple/gi

 *    apple → text to search
 *   g → global flag  (replace all matches) without g, only first match is replaced

 *   i → case-insensitive flag
 *   matches:
 *   apple
 *   Apple
 *   APPLE
 */